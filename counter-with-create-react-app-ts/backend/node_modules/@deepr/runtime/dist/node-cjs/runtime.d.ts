import { PromiseLikeable } from 'core-helpers';
import { Expression } from './expression';
export declare type InvokeExpressionOptions = {
    context?: any;
    authorizer?: Authorizer;
    errorHandler?: ErrorHandler;
};
declare type Authorizer = (key: string, operation: string, params?: any[]) => PromiseLikeable<boolean>;
declare type ErrorHandler = (error: Error) => unknown;
export declare function invokeExpression(target: any, expression: Expression, options?: InvokeExpressionOptions): unknown;
export {};
