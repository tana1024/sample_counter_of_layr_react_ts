"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeValidator = void 0;
const validator_builders_1 = require("./validator-builders");
const validator_1 = require("./validator");
function normalizeValidator(validator, attribute) {
    if (validator_1.isValidatorInstance(validator)) {
        return validator;
    }
    if (typeof validator !== 'function') {
        throw new Error(`The specified validator is not a function (${attribute.describe()})`);
    }
    if (Object.values(validator_builders_1.validators).includes(validator)) {
        throw new Error(`The specified validator is a validator builder that has not been called (${attribute.describe()})`);
    }
    return new validator_1.Validator(validator);
}
exports.normalizeValidator = normalizeValidator;
//# sourceMappingURL=utilities.js.map