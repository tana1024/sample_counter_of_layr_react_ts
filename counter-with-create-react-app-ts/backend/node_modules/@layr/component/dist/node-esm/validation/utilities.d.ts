import { Validator, ValidatorFunction } from './validator';
import type { Attribute } from '../properties';
export declare function normalizeValidator(validator: Validator | ValidatorFunction, attribute: Attribute): Validator;
